---
- hosts: stanford-cs-data-node
  become: true
  gather_facts: yes
  vars:
    server_name: stanford-cs-data-node
    researcher: jure
    user_password: "{{ lookup('password', 'credentials/stanford-cs length=15 encrypt=sha512_crypt') }}"
    vnc_password: "{{ lookup('password', 'credentials/stanford-cs length=15') }}"
    vnc_users:
      - name: jure
        xdisplay: 0
  roles:
    - base-os
    - data-node
    - hostname
    - update-all
    - add-researcher
    - desktop
    - jupyter
  tasks:
  - name: Install Git
    yum: name=git state=present
