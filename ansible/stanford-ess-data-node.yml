---
- hosts: stanford-ess-data-node
  become: true
  gather_facts: yes
  vars:
    server_name: stanford-ess-data-node
    researcher: marshall
    user_password: "{{ lookup('password', 'credentials/stanford-ess length=15 encrypt=sha512_crypt') }}"
    vnc_password: "{{ lookup('password', 'credentials/stanford-ess length=15') }}"
    vnc_users:
      - name: marshall
        xdisplay: 0
  roles:
    - base-os
    - data-node
    - hostname
    - update-all
    - add-researcher
    - desktop
    - jupyter
  tasks:
  - name: Install Git
    yum: name=git state=present
