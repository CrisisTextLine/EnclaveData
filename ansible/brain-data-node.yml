---
- hosts: brain-data-node
  become: true
  gather_facts: yes
  vars:
    server_name: brain-data-node
    researcher: rebekah
    user_password: "{{ lookup('password', 'credentials/brain length=15 encrypt=sha512_crypt') }}"
    vnc_password: "{{ lookup('password', 'credentials/brain length=15') }}"
    vnc_users:
      - name: rebekah
        xdisplay: 0
  roles:
    - base-os
    - data-node
    - hostname
    - update-all
    - add-researcher
    - desktop
    - jupyter
  tasks:
  - name: Install Git
    yum: name=git state=present
