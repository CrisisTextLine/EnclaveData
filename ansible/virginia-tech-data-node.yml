---
- hosts: virginia-tech-data-node
  become: true
  gather_facts: yes
  vars:
    server_name: virginia-tech-data-node
    researcher: stephen
    user_password: "{{ lookup('password', 'credentials/virginia-tech length=15 encrypt=sha512_crypt') }}"
    vnc_password: "{{ lookup('password', 'credentials/virginia-tech length=15') }}"
    vnc_users:
      - name: stephen
        xdisplay: 0
  roles:
    - base-os
    - data-node
    - hostname
    - update-all
    - add-researcher
    - desktop
    - jupyter
  tasks:
  - name: Install Git
    yum: name=git state=present
