---
- hosts: app-state-data-node
  become: true
  gather_facts: yes
  vars:
    server_name: app-state-data-node
    researcher: jennifer
    user_password: "{{ lookup('password', 'credentials/app-state length=15 encrypt=sha512_crypt') }}"
    vnc_password: "{{ lookup('password', 'credentials/app-state length=15') }}"
    vnc_users:
      - name: jennifer
        xdisplay: 0
  roles:
    - base-os
    - data-node
    - hostname
    - update-all
    - add-researcher
    - desktop
    - jupyter
  tasks:
  - name: Install Git
    yum: name=git state=present
